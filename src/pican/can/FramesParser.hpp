#pragma once

// TODO basically what a dbc file would do, but we don't want to bother with that for now (maybe one day)
//  take a FramesBuffer "snapshot", ie 1 frame per id for each id, and parse it to the information we care
//  about, just use hardcoded shit for now but in a way that means we can make it flexible using a dbc file later
