#pragma once

// TODO the highest level entity that manages everything regarding the bus including the FramesBuffer,
//  the thread, the filters etc, users should mostly use this class

namespace pican::can {}
