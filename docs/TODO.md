# TODO

Write every part of the build, compile, check, run, test etc in bash scripts which cmake can use if we want
Create full checker in CMake to check all files (no formatting though) ?
Create a formatting script to format files ?
Create git pre-hook to ensure files are formatted and safe before commiting, fails if not, prompting dev to go back 
and change things
Sanitizers
Create Hoare requirements (most if not all will be at runtime, possibly even only for debug builds for performance 
reasons)
Unit testing stuff
Yocto qemu, docker stuff 